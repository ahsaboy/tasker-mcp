{
  "_comment": "VSCode Continue extension MCP config. Merge this into ~/.continue/config.json under the experimental.modelContextProtocolServers array.",
  "experimental": {
    "modelContextProtocolServers": [
      {
        "transport": {
          "type": "streamableHttp",
          "url": "http://127.0.0.1:8000/mcp"
        }
      }
    ]
  }
}
